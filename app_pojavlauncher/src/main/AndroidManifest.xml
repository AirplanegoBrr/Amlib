<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk
        tools:overrideLibrary="dev.alexytomi.androidnsbypass"/>

    <uses-feature android:glEsVersion="0x00020000" />
    <uses-feature
        android:name="android.hardware.type.pc"
        android:required="false" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- OpenXR on Meta Quest, used by the "Launch in VR" mode (vr.VRGameActivity) -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="false"
        android:version="1" />
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR" />
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR_SYSTEM" />
    <meta-data android:name="android.game_mode_config"
        android:resource="@xml/game_mode_config" />
    <!-- App-wide settings (Application class, icon, theme, process) belong to the app using
         Amlib (AmethystXR-Menu). Only what the game needs in any app stays here. -->
    <application android:allowNativeHeapPointerTagging="false">
        <!-- Lets a Godot app (AmethystXR-Menu) use Amlib as its "Amethyst" singleton -->
        <meta-data
            android:name="org.godotengine.plugin.v2.Amethyst"
            android:value="net.kdt.pojavlaunch.vr.menu.VRMenuPlugin" />
        <activity
            android:theme="@style/AppTheme"
            android:name=".MissingStorageActivity"
            android:exported="false" />

        <meta-data
            android:name="android.max_aspect"
            android:value="ratio_float" />

        <!-- The phone app adds the launcher intent filter to this -->
        <activity
            android:theme="@style/AppTheme"
            android:name=".TestStorageActivity"
            android:exported="false"
            android:label="@string/app_short_name"
            android:launchMode="singleTop" />
        <activity
            android:theme="@style/AppTheme"
            android:name=".LauncherActivity"
            android:label="@string/app_short_name"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:theme="@style/AppTheme"
            android:name=".ImportControlActivity"
            android:configChanges="keyboard|keyboardHidden"
            android:exported="true"
            android:launchMode="singleInstance"
            android:windowSoftInputMode="stateVisible">
            <intent-filter
                android:label="@string/import_control_label"
                android:scheme="content"
                tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="application/json" />
                <data android:mimeType="text/json" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FatalErrorActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity android:name=".ShowErrorActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".ExitActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:theme="@style/AppTheme"
            android:process=":gui_installer"
            android:name=".JavaGUILauncherActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|keyboard|navigation|uiMode"
            android:screenOrientation="sensorLandscape" />
        <activity
            android:theme="@style/AppTheme"
            android:name=".CustomControlsActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation"
            android:exported="false"
            android:screenOrientation="sensorLandscape">
            <intent-filter>
                <action android:name=".CustomControlsActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/AppTheme"
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|keyboard|navigation|uiMode"
            android:launchMode="singleTop"
            android:process=":game"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="sensorLandscape" />
        <!-- Immersive game screen for Meta Quest; the launcher stays a 2D panel -->
        <activity
            android:theme="@style/AppTheme"
            android:name=".vr.VRGameActivity"
            android:configChanges="density|keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:exported="false"
            android:launchMode="singleTask"
            android:process=":game"
            android:resizeableActivity="false"
            android:screenOrientation="landscape"
            android:taskAffinity="${applicationId}.vr">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.oculus.intent.category.VR" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest2|questpro|quest3|quest3s" />

        <provider
            android:name=".scoped.FolderProvider"
            android:authorities="${applicationId}.scoped.gamefolder"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <service android:name=".services.ProgressService"
            android:foregroundServiceType="dataSync"/>
        <service android:name=".services.GameService" android:process=":game"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="This application is intended to run a 3rd-party piece of software which cannot be managed using the activity's saved instance state. This service is used for background persistence of this piece of software."/>
        </service>
    </application>
    <queries>
        <package android:name="net.kdt.pojavlaunch.ffmpeg"/>
        <!-- Lets the OpenXR loader find the active runtime -->
        <provider android:authorities="org.khronos.openxr.runtime_broker;org.khronos.openxr.system_runtime_broker" />
        <intent>
            <action android:name="org.khronos.openxr.OpenXRRuntimeService" />
        </intent>
        <intent>
            <action android:name="org.khronos.openxr.OpenXRApiLayerService" />
        </intent>
    </queries>
</manifest>